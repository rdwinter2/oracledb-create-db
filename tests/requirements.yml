---

# Note: The git server resolves to a location on the LAN
# using /etc/resolv.conf or similar means
# The repos on the git server are maintained as part of the
# CI/CD pipeline
- src: http://git/oracledb/oracledb-el7-kickstart
- src: http://git/oracledb/oracledb-el7-prerequisites

...